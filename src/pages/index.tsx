import DefaultLayout from '@/components/ui/DefaultLayout';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <DefaultLayout>
      <h3>Grizzly Hauling</h3>
    </DefaultLayout>
  );
};

export default Home;
